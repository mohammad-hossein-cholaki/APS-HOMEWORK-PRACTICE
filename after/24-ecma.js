const myMath = {
    PI: 3.14,
    square(num) {
        return num * num;
    },
    cube(num) {
        return num * num * num;
    },
};

// you can use the syntax sugar of writing methods in objects
